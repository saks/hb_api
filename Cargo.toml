[workspace]

members = [
    "octo-budget-api",
    "octo-budget-api-server",
    "octo-budget-lib",
    "octo-budget-frontend",
    "middlewares",
    "db_seed",
    "models",
]

[profile.release.package.octo-budget-frontend]
# Tell `rustc` to optimize for small code size.
opt-level = 's'
